<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <!-- Custom CSS -->
        <link rel="stylesheet" href="style.css" />
        <title>Cards</title>
    </head>
    <body>
        <h1>Home Page</h1>
        <%# Create a card for each data item %>
        <% data.forEach(el => { %>
            <%- include("partials/card", {el: el}) %>
        <% }); %>
    </body>
</html>
